
var domain_a = 'http://domain.a.com';
var domain_b = 'http://domain.b.com';
var path_to_root = '/github/dive-into-html5/cross-domain/iframe';

function getRandom(min, max){
  return Math.floor(Math.random() * (max - min)) + min;
}